---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: bash
    tag: 4.4

inputs:
  - name: elasticsearch
  - name: x-pack-elasticsearch

run:
  path: echo
  args: [hello world]
